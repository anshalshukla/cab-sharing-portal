import { DefaultTheme } from 'styled-components';
import { Theme } from '../../admin-bro-options.interface';
declare const combineStyles: (newTheme: Partial<Theme>) => DefaultTheme;
export default combineStyles;
