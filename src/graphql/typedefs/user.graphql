type User {
  _id: String!
  name: String
  email: String
}

type Token {
  token: String
}

type Query {
  all_users:[User!]
  user(_id: String!): User
}

type Mutation {
  oAuth2Google(
    code: String!
  ): Token
}