type User {
  _id: String!
  name: String
  email: String
  bits_id: String
}

type Token {
  token: String
}

type Query {
  user(_id: String!): User
}

type Mutation {
  oAuth2Google(
    code: String!
  ): Token
}